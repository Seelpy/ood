#pragma once

#include "./../IView.h"
#include "./../BaseView.h"

class MenuView: public BaseView {
public:
    explicit MenuView(Rect frame): BaseView(frame){

    }

    void Show(ICanvas &canvas) override
    {

    }

};